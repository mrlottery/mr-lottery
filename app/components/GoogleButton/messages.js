/*
 * GoogleButton Messages
 *
 * This contains all the text for the GoogleButton component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.components.GoogleButton';

export default defineMessages({
  home: {
    id: `${scope}.signIn`,
    defaultMessage: 'Sign In With Google ',
  },
});
