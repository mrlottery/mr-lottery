/*
 * Concepts Messages
 *
 * This contains all the text for the Concepts container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Concepts';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Concepts container!',
  },
});
