import GoogleButton from '../../components/GoogleButton'

const Input = GoogleButton.input`
  outline: none;
  border: none;
  border-bottom: 1px dotted #999;
  background-color: transparent;
`;

export default Input;
